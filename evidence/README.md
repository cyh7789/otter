# Coding Agent Evidence

This project was built using **Claude Code** powered by **Claude Agent SDK** (Anthropic) as the primary coding agent, with **Codex** (OpenAI) for QA review.

## Evidence

### Git Commit History

See [`git-log-full.txt`](./git-log-full.txt) for the complete commit history. Key observations:

- **20 commits** from initial scaffold to submission
- Commit messages follow conventional commit format, generated by Claude Code
- Notable commits authored via Claude Code:
  - `deb02eb` — Initial LangGraph multi-agent supervisor + UiPath integration
  - `5bf5c09` — Centralized Pydantic contracts for v1 happy path
  - `53d1c17` — Replace 4-node starter with full 10-node BPMN graph
  - `84b0684` — Wire diagnosis_agent live branch to real Gemini call
  - `525e3b3` — Fix UiPath Cloud import (identified by Codex review)
  - `0b063be` — Argparse + structured error guard (identified by Codex review)

### Codex QA Review

Codex was used to review the codebase after major milestones. Two critical findings were actioned:

1. **Codex critical #2** (`525e3b3`): UiPath Cloud config was missing required fields, preventing import
2. **Codex high #3** (`0b063be`): Main entrypoint lacked structured error handling

### Development Workflow

1. Claude Code authored all production code (graph, schemas, agents, manifests)
2. Codex reviewed each major milestone for correctness and platform compliance
3. Claude Code actioned Codex findings and committed fixes
4. Demo video (voiceover script, GSAP composition, HyperFrames render) also produced via Claude Code

### Disclosure

Per UiPath AgentHack bonus criteria, this evidence is preserved to demonstrate genuine use of coding agents throughout the development lifecycle.
